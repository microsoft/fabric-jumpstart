import { makeStyles } from '@fluentui/react-components';
import spacingToken from '@styles/spacing';

export const useStyles = makeStyles({
  dropImg: {
    maxWidth: '100%',
    margin: `${spacingToken.spacing16} 0`,
  },
});
